{
  "name": "kizana",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "lint": "eslint --ext .astro,.js,.ts .",
    "format": "prettier --write .",
    "prestart": "rm -rf ./dist && pnpm lint && pnpm format",
    "prebuild": "rm -rf ./dist && pnpm lint && pnpm format",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.0.0",
    "astro": "^3.2.0",
    "better-sqlite3": "^8.6.0",
    "tailwindcss": "^3.0.24"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.5",
    "@typescript-eslint/parser": "^6.7.3"
  }
}
